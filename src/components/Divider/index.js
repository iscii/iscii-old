import "./index.scss";

const Divider = (props) => {
	/**
	 * !props
     * parameterize length, thickness, etc
	 * 
	 */

	return (
		/* add tabbed components for projects */
        <div className="idivider"></div>
	);
};

export default Divider;
